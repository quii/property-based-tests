# await node cli

Just messing around with using `async/await`

## notes

- Wrapped stuff to read from the command line into `async/await` functions in `cli.ts`
- Put the very basic application business logic into `app.ts`

## running

`$ nvm use`

`$ yarn`

`$ yarn start`